<h1><%=  @category.name %></h1>
<p><%= @category.description %></p>
<div class="flex">
  <%= button_to 'Delete', @category, method: 'delete', class: 'offset-right--xs' %>
  <%= link_to 'Edit', edit_category_path(@category)  %>
</div>
